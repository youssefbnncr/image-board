script(type='text/javascript', src='/menu.js', defer)
header
    h1 temp
    a(href="/"): h1.header-title Tochi
    .auth-buttons
       -if (isAuthenticated==true)
        a(href="/user/user-settings"): img(src=`/avatar/${user.avatar}`, alt="avatar", class='avatar-small')
        a(href="/user/user-settings"): h3 #{user.username}
        .menu
            .burger 
            ul
                li Profile
                li: a(href="/user/user-settings") Settings
                li
                    form(action="/user/log-out", method="post"): button(type="submit", class='log-out') Log-out
       -else if (current_path !== '/register' && current_path !== '/login' && isLoged!==true)
        a(href="/user/register" class="btn register") Register
        a(href="/user/login" class="btn log-in") Log-in
       -else if (current_path !== '/register' && isLoged!==true)
        a(href="/user/register" class="btn register") Register
       -else if (current_path !== '/login' && isLoged!==true)
        a(href="/user/login" class="btn log-in") Log-in
